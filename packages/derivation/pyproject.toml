[project]
name = "fva-derivation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fva-core",
  "sf-hamilton", "scipy",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/derivation"]
