# ADR-001: Broadcast come tipo di camera primario

**Status:** Accettata

Serie A è l'obiettivo e non c'è accesso a footage grandangolare. Osservabilità parziale e diritti sono accettati; `@config.when` tiene aperta la variante `fixed`.
